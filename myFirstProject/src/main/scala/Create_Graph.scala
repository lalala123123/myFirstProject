package Graph

import org.apache.spark.rdd.RDD
import com.google.common.hash.Hashing

object Create_Graph {

//  def create_graph_call(call:RDD[Terminal_Date]): Unit ={
//    val vertices = call.map(date=>(hashId(date.send),date,send))
//  }
//  def hashId(str:String) ={
//    Hashing.md5().hashString(str).asLong()
//  }
}
